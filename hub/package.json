{
  "name": "matcraft-hub",
  "version": "1.0.0",
  "description": "MatCraft Hub — Personal service dashboard",
  "scripts": {
    "start": "tsx server.ts",
    "build:client": "cd client && npm run build",
    "postinstall": "cd client && npm install",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.1",
    "express-session": "^1.18.1",
    "tsx": "^4.19.0"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/express": "^5.0.0",
    "@types/express-session": "^1.18.1",
    "typescript": "^5.7.0"
  }
}
